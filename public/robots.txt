User-agent: *
Allow: /

# Listing pages can be narrowed by distance from the visitor (?min= / ?max=).
# Those results depend on who is looking, and every variant declares the
# unfiltered listing as its canonical, so there is nothing for a crawler to
# gain from them.
Disallow: /*?*min=
Disallow: /*?*max=

# This host serves only the public site. The admin panel and the API are
# separate hosts and carry their own noindex controls; pages that should stay
# out of search here (customer registration, missing listings) say so with a
# robots meta tag, which a Disallow would hide from crawlers.

Sitemap: https://www.morselv.com/sitemap.xml
